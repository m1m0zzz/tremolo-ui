---
'@tremolo-ui/vue': minor
---

Add `@tremolo-ui/vue`, the Vue 3 wrapper. This first release carries the
composables — `useDrag`, `useDragValue`, `useWheel`, `useLongPress`,
`useDropZone`, `useMIDIAccess`, `useMIDIInput` and `useMIDIMessage`.
