export type KeyBoardShortcuts = {
  keys: string[],
  flags?: {
    naturalOnly?: boolean
  },
}

export const Shortcuts = {
  HOME_ROW: {
    keys: ['a', 'w', 's', 'e', 'd', 'f', 't', 'g', 'y', 'h', 'u', 'j', 'k', 'o', 'l', 'p', ';']
  }
}
