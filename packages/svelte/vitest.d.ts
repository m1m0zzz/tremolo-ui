// Types for the globals `vitest.config.ts` turns on. A reference rather than
// `compilerOptions.types`, which would switch off the automatic inclusion of
// every other @types package.
/// <reference types="vitest/globals" />
