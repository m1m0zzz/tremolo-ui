import CodeBlock from '@theme/CodeBlock'
import { PropsTable } from '@site/src/components/ApiReference'
import GitHubLink, { StyleGitHubLink } from '@site/src/components/GitHubLink'
import basic from '!!raw-loader!@site/examples/components/xy-pad/basic'
import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'
import svelteBasic from '!!raw-loader!@site/examples/svelte/XYPad.svelte'
import vueBasic from '!!raw-loader!@site/examples/vue/XYPad.vue'

# XYPad

2 次元のスライダーのコンポーネント。

<GitHubLink
  path='packages/react/src/components/XYPad/index.tsx'
/>
<StyleGitHubLink
  path='shared/css/XYPad.module.css'
/>

<Tabs groupId="framework" queryString>
<TabItem value="react" label="React">

<CodeBlock
  jsx
  live
  showCode
  sourcePath='examples/components/xy-pad/basic.tsx'
>{basic}</CodeBlock>

</TabItem>
<TabItem value="svelte" label="Svelte">

<CodeBlock language="html" title="App.svelte">{svelteBasic}</CodeBlock>

</TabItem>
<TabItem value="vue" label="Vue">

<CodeBlock language="html" title="App.vue">{vueBasic}</CodeBlock>

</TabItem>
</Tabs>

## インポート {#import}

<Tabs groupId="framework" queryString>
<TabItem value="react" label="React">

```jsx
import { XYPad } from '@tremolo-ui/react'
```

</TabItem>
<TabItem value="svelte" label="Svelte">

```js
import { XYPad } from '@tremolo-ui/svelte'
```

</TabItem>
<TabItem value="vue" label="Vue">

```js
import { XYPad, XYPadArea, XYPadThumb } from '@tremolo-ui/vue'
```

</TabItem>
</Tabs>

## API リファレンス {#api-reference}

各パートには、そのパート独自の props を載せています。これに加えて、描画する要素の属性（`className` や `style` など）も受け取ります。

Svelte / Vue でも props は同じで、書き方だけがそれぞれの流儀になります（`className` ではなく `class`、`bind:value` や `v-model`、中身は snippet やスロットなど）。[Svelte](/docs/tutorials/svelte) と [Vue](/docs/tutorials/vue) を参照してください。

状態は data 属性として要素に乗っているので、セレクタ 1 つで届きます（[スタイリング](/docs/tutorials/styling)を参照）。属性はその状態が on の間だけ付きます。

### Root

両軸の値と設定を持ち、中のパートに渡します。

<PropsTable of="XYPadProps" />

| Data 属性 | 値 |
| --- | --- |
| `[data-disabled]` | `disabled` のときに付きます |
| `[data-readonly]` | `readonly` のときに付きます |

### Area

つまみが動く面。`XYPad.Thumb` はこの中に置きます。

幅・高さ・背景色は CSS または `style` で指定します。`<div>` の属性も受け取ります。

### Thumb

つまみ。中にある軸ごとの 2 つの視覚的に隠れた range input が、フォーカスを受け取り ARIA を持ちます。

子はつまみの中に描かれます。つまみはどちらにしても 1 つの要素なので、子はつまみを置き換えるのではなく、その上に載る飾りです。つまみ自体の見た目は `className` と `style` で変えます。

<PropsTable of="XYPadThumbProps" />

| カスタムプロパティ | 書き込む prop | 決めるもの |
| --- | --- | --- |
| `--color` | `color` | つまみの色。 |

| Data 属性 | 値 |
| --- | --- |
| `[data-disabled]` | `disabled` のときに付きます |
| `[data-readonly]` | `readonly` のときに付きます |
