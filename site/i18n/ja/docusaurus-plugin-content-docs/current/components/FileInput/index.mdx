import CodeBlock from '@theme/CodeBlock'
import { PropsTable } from '@site/src/components/ApiReference'
import GitHubLink, { StyleGitHubLink } from '@site/src/components/GitHubLink'
import basic from '!!raw-loader!@site/examples/components/file-input/basic'
import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'
import svelteBasic from '!!raw-loader!@site/examples/svelte/FileInput.svelte'
import vueBasic from '!!raw-loader!@site/examples/vue/FileInput.vue'

# FileInput

選ばれたファイルを `File` として渡すコンポーネント。

読み込むのは呼び出し側の仕事です。音声ファイルなら `decodeAudioData`、プリセットなら `JSON.parse` へ。このコンポーネントは、ブラウザがバイト列を渡してきたところで止まります。

<GitHubLink
  path='packages/react/src/components/FileInput/index.tsx'
/>
<StyleGitHubLink
  path='shared/css/FileInput.module.css'
/>

<Tabs groupId="framework" queryString>
<TabItem value="react" label="React">

<CodeBlock
  jsx
  live
  showCode
  sourcePath='examples/components/file-input/basic.tsx'
>{basic}</CodeBlock>

</TabItem>
<TabItem value="svelte" label="Svelte">

<CodeBlock language="html" title="App.svelte">{svelteBasic}</CodeBlock>

</TabItem>
<TabItem value="vue" label="Vue">

<CodeBlock language="html" title="App.vue">{vueBasic}</CodeBlock>

</TabItem>
</Tabs>

## インポート {#import}

<Tabs groupId="framework" queryString>
<TabItem value="react" label="React">

```jsx
import { FileInput } from '@tremolo-ui/react'
```

</TabItem>
<TabItem value="svelte" label="Svelte">

```js
import { FileInput } from '@tremolo-ui/svelte'
```

</TabItem>
<TabItem value="vue" label="Vue">

```js
import { FileInput, FileInputTrigger } from '@tremolo-ui/vue'
```

</TabItem>
</Tabs>

## このコンポーネントが引き受けること {#what-it-handles-for-you}

**`accept` は受け取るときにもう一度照合します。** ブラウザはこの属性をピッカーへのヒントとして扱うので、「すべてのファイル」に切り替えれば何でも選べます。一致しなかったものは `onChange` ではなく `onReject` に届くので、黙って何も起きない代わりに理由を表示できます。

**同じファイルを 2 回選べます。** ファイル入力は選択が変わらないと `change` イベントを発火しないので、直前に選んだファイルをもう一度選んでも何も起きないはずでした。読み取った時点で value を空にしています。

**コントロールは今もネイティブの input です。** ツリーの中に、見えないけれどタブ順には残る形で置いてあります。フォーカスを取り、Enter でピッカーを開き、アクセシブルな名前を持つのはこの要素です。`FileInput.Trigger` はその `<label>` なので、独自の JavaScript 無しにクリックがピッカーへ届きます。

フォーカスが当たるのは見えない要素なので、フォーカスリングは親から描いてください。デモのテーマは `.root` の `:focus-within` でそうしています。

input の ARIA 属性は、`Root` が描くラッパーに残さず `Root` が受け取ってコントロールへ渡します。「WAV / AIFF、10 MB まで」のような補足は `aria-describedby` で指し、`FileInput.Trigger` を使わず名前を取れない場合は `aria-label` で付けられます。

## ドロップも受け取る {#receiving-a-drop-as-well}

`FileInput` が答えるのはクリックです。ページにドロップされたファイルを受け取るには [`DropZone`](/docs/components/DropZone) を見てください。同じ `File[]` を報告します。

## API リファレンス {#api-reference}

各パートには、そのパート固有の props があります。描画する要素の属性（`className` や `style` など）も受け取ります。

Svelte / Vue でも props は同じで、書き方だけがそれぞれの流儀になります（`className` ではなく `class`、`bind:value` や `v-model`、中身は snippet やスロットなど）。[Svelte](/docs/tutorials/svelte) と [Vue](/docs/tutorials/vue) を参照してください。

状態は data 属性として要素に付くので、セレクタひとつで届きます（[スタイリング](/docs/tutorials/styling)）。属性は、その状態が on のときだけ存在します。

### Root

設定と、隠された `<input type="file">` を持ち、選ばれたものを報告します。

<PropsTable of="FileInputProps" />

| Data attribute | Values |
| --- | --- |
| `[data-disabled]` | `disabled` が設定されているときに存在します |

### Trigger

ピッカーを開き、スクリーンリーダー向けに input へ名前を付けます。`<label>` として描画されます。

<PropsTable of="FileInputTriggerProps" />

| Data attribute | Values |
| --- | --- |
| `[data-disabled]` | `Root` に `disabled` が設定されているときに存在します |
