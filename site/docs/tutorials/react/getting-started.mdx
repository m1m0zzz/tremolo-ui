---
sidebar_position: 1
title: Getting Started
---

# Getting Started with React

## Install @tremolo-ui/react

1. Install tremolo-ui

```bash npm2yarn
npm install @tremolo-ui/react
```

2. Install dependecies

```bash npm2yarn
npm install react react-dom @emotion/react
```

## First Step

Below is an example of a simple knob component.

```jsx live
// expand begin
import { Knob } from '@tremolo-ui/react'
import { useState } from 'react'
// expand end

function App() {
  const [value, setValue] = useState(64)

  return (
    <div style={{
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      flexDirection: 'column'
    }}>
      <Knob
        value={value}
        min={0}
        max={100}
        size={50}
        onChange={(v) => setValue(v)}
      />
      {value}
    </div>
  )
}

// expand begin
export default App
// expand end
```

Check out the look and feel in the storybook!

[Storybook - React Knob](https://tremolo-ui-sb-react.vercel.app/?path=/docs/react-components-knob--docs)

