import CodeBlock from '@theme/CodeBlock'
import GitHubLink, { StyleGitHubLink } from '@site/src/components/GitHubLink'
import basic from '!!raw-loader!@site/examples/components/xy-pad/basic'

# XYPad

Two-dimensional slider component.

<GitHubLink
  path='packages/react/src/components/XYPad/index.tsx'
/>
<StyleGitHubLink
  path='packages/shared/css/XYPad.module.css'
/>

## Import

```jsx
import { XYPad } from '@tremolo-ui/react'
```

## Data attributes

State is on the element as an attribute, so it is a selector away — see
[Styling](/docs/tutorials/styling). An attribute is only there while its
state is on.

| Attribute | Part | Present when |
| --- | --- | --- |
| `data-disabled` | `XYPad.Root`, `XYPad.Thumb` | `disabled` is set |
| `data-readonly` | `XYPad.Root`, `XYPad.Thumb` | `readonly` is set |

## Examples

### Basic

<CodeBlock
  jsx
  live
  showCode
  sourcePath='examples/components/xy-pad/basic.tsx'
>{basic}</CodeBlock>
