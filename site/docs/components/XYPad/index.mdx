import CodeBlock from '@theme/CodeBlock'
import { PropsTable } from '@site/src/components/ApiReference'
import GitHubLink, { StyleGitHubLink } from '@site/src/components/GitHubLink'
import basic from '!!raw-loader!@site/examples/components/xy-pad/basic'
import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'
import svelteBasic from '!!raw-loader!@site/examples/svelte/XYPad.svelte'
import vueBasic from '!!raw-loader!@site/examples/vue/XYPad.vue'

# XYPad

Two-dimensional slider component.

<GitHubLink
  path='packages/react/src/components/XYPad/index.tsx'
/>
<StyleGitHubLink
  path='shared/css/XYPad.module.css'
/>

<Tabs groupId="framework" queryString>
<TabItem value="react" label="React">

<CodeBlock
  jsx
  live
  showCode
  sourcePath='examples/components/xy-pad/basic.tsx'
>{basic}</CodeBlock>

</TabItem>
<TabItem value="svelte" label="Svelte">

<CodeBlock language="html" title="App.svelte">{svelteBasic}</CodeBlock>

</TabItem>
<TabItem value="vue" label="Vue">

<CodeBlock language="html" title="App.vue">{vueBasic}</CodeBlock>

</TabItem>
</Tabs>

## Import

<Tabs groupId="framework" queryString>
<TabItem value="react" label="React">

```jsx
import { XYPad } from '@tremolo-ui/react'
```

</TabItem>
<TabItem value="svelte" label="Svelte">

```js
import { XYPad } from '@tremolo-ui/svelte'
```

</TabItem>
<TabItem value="vue" label="Vue">

```js
import { XYPad, XYPadArea, XYPadThumb } from '@tremolo-ui/vue'
```

</TabItem>
</Tabs>

## API Reference {#api-reference}

Each part lists the props of its own. It also takes the attributes of the
element it renders, such as `className` and `style`.

In Svelte and Vue the props are the same, written their way — `class` rather
than `className`, `bind:value` or `v-model`, snippets or slots for content. See
[Svelte](/docs/tutorials/svelte) and [Vue](/docs/tutorials/vue).

State is on the element as a data attribute, so it is a selector away — see
[Styling](/docs/tutorials/styling). An attribute is only there while its state
is on.

### Root

Holds the value of both axes and the settings, and passes them to the parts
inside.

<PropsTable of="XYPadProps" />

| Data attribute | Values |
| --- | --- |
| `[data-disabled]` | Present when `disabled` is set |
| `[data-readonly]` | Present when `readonly` is set |

### Area

The surface the thumb moves across. `XYPad.Thumb` goes inside it.

Set its size and background with CSS or `style`. It takes the attributes of a
`<div>`.

### Thumb

The handle. Two visually hidden range inputs inside it, one per axis, take the
focus and carry the ARIA.

Its children are drawn inside it. The thumb is one element either way, so what
is passed there is decoration on top of it rather than a replacement for it —
`className` and `style` are how its own appearance is changed.

<PropsTable of="XYPadThumbProps" />

| Custom property | Written by | What it sets |
| --- | --- | --- |
| `--color` | `color` | Colour of the thumb. |

| Data attribute | Values |
| --- | --- |
| `[data-disabled]` | Present when `disabled` is set |
| `[data-readonly]` | Present when `readonly` is set |
